<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="me.dmjohnson.wgu.c196.CourseViewActivity">
    <item
        android:id="@+id/alert_start_date"
        android:title="@string/alert_start_date"
        app:actionLayout="@layout/switch_item"
        app:showAsAction="never"
        android:onClick="onAlertStartDateAction"/>
    <item
        android:id="@+id/alert_end_date"
        android:title="@string/alert_end_date"
        app:actionLayout="@layout/switch_item"
        app:showAsAction="never"
        android:onClick="onAlertEndDateAction"/>
    <item
        android:id="@+id/share_note"
        android:title="@string/share_course_notes"
        app:showAsAction="never"
        android:onClick="onShareNoteAction"/>
    <item
        android:icon="@drawable/ic_delete_red"
        android:title="@string/delete_course"
        app:showAsAction="never"
        android:onClick="onDeleteAction"/>
</menu>
